{
  "name": "rustdesk-portal",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "check:config": "node scripts/check-config.mjs",
    "deploy": "bash scripts/deploy.sh"
  },
  "dependencies": {
    "@fontsource/inter": "^5.3.0",
    "@fontsource/inter-tight": "^5.3.0",
    "astro": "^7.3.4"
  },
  "allowScripts": {
    "esbuild": true
  }
}
